# Testing
# pytest 9.1.1 (was 7.4.0) fixes PYSEC-2026-1845; plugin versions bumped
# alongside it for compatibility (#705). Verified: full fast suite (2,776
# tests) plus --splits/--group/--cov flags used by CI all pass unchanged.
pytest==9.1.1
pytest-cov==7.1.0
pytest-mock==3.15.1
pytest-xdist==3.8.0
# Partitions the suite across CI runners (--splits/--group); see the sharded
# `test` job in .github/workflows/ci.yml.
pytest-split==0.11.0
hypothesis>=6.0.0
factory-boy==3.3.0
Faker==19.3.0

# Code Quality
ruff==0.4.3
black==23.9.1
isort==5.12.0
mypy==1.5.1
# Type stubs for the CI `mypy` lint job. Without these, mypy reports
# "Library stubs not installed for 'yaml'" (not suppressed by
# --ignore-missing-imports because the stub is known to exist on PyPI),
# keeping the lint job red. See #333.
types-PyYAML==6.0.12.20260518
pylint==3.0.2
flake8==6.1.0
pre-commit==3.3.3

# Dependency vulnerability scanning (#705)
pip-audit==2.10.1

# Docs / Dev tools
# mkdocs-material 9.7.7 (was 9.3.1) fixes PYSEC-2026-3864 and requires
# mkdocs>=1.6; pymdown-extensions pinned explicitly (was transitive) at its
# own fix version to clear PYSEC-2026-3609/PYSEC-2026-3654 (#705).
mkdocs==1.6.1
mkdocs-material==9.7.7
pymdown-extensions==11.0.1
mkdocstrings-python==1.5.0
jupyter==1.0.0
ipython==9.17.1
ipdb==0.13.13
