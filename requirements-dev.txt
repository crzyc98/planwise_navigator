# Testing
pytest==9.1.1
pytest-cov==7.1.0
pytest-mock==3.15.1
pytest-xdist==3.8.0
# Partitions the suite across CI runners (--splits/--group); see the sharded
# `test` job in .github/workflows/ci.yml.
pytest-split==0.11.0
hypothesis>=6.0.0
factory-boy==3.3.3
Faker==40.39.0

# Code Quality
ruff==0.16.8
black==26.5.1
isort==9.0.1
mypy==2.3.1
# Type stubs for the CI `mypy` lint job. Without these, mypy reports
# "Library stubs not installed for 'yaml'" (not suppressed by
# --ignore-missing-imports because the stub is known to exist on PyPI),
# keeping the lint job red. See #333.
types-PyYAML==6.0.12.20260906
pylint==4.0.8
flake8==7.3.0
pre-commit==4.6.2

# Docs / Dev tools
mkdocs==1.6.1
mkdocs-material==9.7.7
mkdocstrings-python==2.0.8
jupyter==1.1.1
ipython==9.17.1
ipdb==0.13.13
